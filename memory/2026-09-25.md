# 2026-09-25

## 记忆维护

巩固 09-22 / 09-24 两天的 OpenClaw + Tim'Radio 排障工作：

- **`memory/topics/local-services.md` 新建** —— 本机两套常驻服务的完整上下文：
  拓扑与端口、Node 依赖、网络前提（只有 fish.audio 需要翻墙，靠 ZionLadder 的 1097）、
  故障对照表、`launchctl disabled` 那个核心坑、6 月到 9 月的事件线、遗留待办、诊断入口速查。
- **`04-MEMORY.md`**：
  - Technical Knowledge 加「本机服务排障通则」——只留可迁移的判据
    （先验第一跳 / Control UI 是 token 不是密码 / build-mismatch 要硬刷新 /
    多 session 可能同时在动同一服务）
  - Ongoing Context 加两套服务的当前状态与遗留项
- 未新建额外的日志层级，事件经过已在 09-22、09-24 两份日志里，topic 只做综合不搬家。

**这轮沉淀出的最有用一条**：服务"在却用不了"时，`launchctl print-disabled` 比
`launchctl list` 更值得先看 —— 后者会骗人（job 没了就是没了，但 disabled 的 job
连 `load` 都返回成功）。
